pipeline {
    agent any

    stages {

        stage("build"){
            steps {
                echo "Build stage"
                echo "Build stage for auto trigger"
            }
        }

        stage("test"){
            steps {
                echo "test stage for auto trigger"
            }
        }

        stage("deploy"){
            steps {
                echo "deploy stage for auto trigger"
            }
        }
    }
}
