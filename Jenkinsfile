pipeline {
    agent any

    stages {

        stage("build"){
            steps {
                echo "Build stage in dev branch"
            }
        }

        stage("test"){
            steps {
                echo "test stage in dev branch"
            }
        }

        stage("deploy"){
            steps {
                echo "deploy stage in dev branch"
            }
        }
    }
}
